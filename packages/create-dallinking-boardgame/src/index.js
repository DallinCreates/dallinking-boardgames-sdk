/**
 * create-dallinking-boardgame
 * Scaffolding tool for creating dallinking boardgames
 */

export function createProject(name) {
  return {
    name,
    version: '0.0.1',
  };
}

export default {
  createProject,
};
