/**
 * @dallinking/boardgame-client
 * Client-side utilities for dallinking boardgames
 */

/**
 * Initialize client utilities
 * @returns {Object} Client utilities
 */
export function initClient() {
  return {
    version: '0.0.1',
  };
}

export default {
  initClient,
};
