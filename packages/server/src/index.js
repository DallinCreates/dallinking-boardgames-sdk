/**
 * @dallinking/boardgame-server
 * Server-side utilities for dallinking boardgames
 */

/**
 * Initialize server utilities
 * @returns {Object} Server utilities
 */
export function initServer() {
  return {
    version: '0.0.1',
  };
}

export default {
  initServer,
};
