{
  "name": "dallinking-boardgames-sdk",
  "version": "1.0.1",
  "private": true,
  "author": "Dallin King",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "prepublishOnly": "npm run build --workspaces"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}